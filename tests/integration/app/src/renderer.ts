import { ReactronxRenderer } from "@reactronx/renderer";

const renderer = new ReactronxRenderer();

// Example of sending a command on load, or reacting to events
console.log("Renderer attached", renderer);
